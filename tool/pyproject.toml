[project]
name = "retentionfrac"
version = "1.0.0"
description = "Identity-retention fraction for paired tumor-adjacent expression contrasts"
requires-python = ">=3.9"
dependencies = ["numpy>=1.20"]
[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
