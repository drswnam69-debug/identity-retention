# Runtime requirements for the analysis pipeline and the retentionfrac package.
#
# statsmodels is used only by the independent cross-check of this study's own
# statistics (26_independent_stats_check.py) and by the two test files that
# check the hand-written implementations against it. Everything else is needed
# by the pipeline itself. lifelines was listed here in earlier versions and is
# not imported anywhere; the survival code is in code/survival.py.
numpy>=1.20
pandas>=1.3
scipy>=1.7
matplotlib>=3.4
Pillow>=9.0
statsmodels>=0.13
fonttools>=4.0
